{% extends 'admin/SubTwig/base.html.twig' %}

{% block title %}{{title}}{% endblock %}

{% block body %}
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <strong>Edit Category</strong>
                <a href="{{ path('user_index') }}" class="btn btn-secondary btn-sm float-right">
                    <i class="fa fa-arrow-left"></i> Back To List
                </a>
            </div>
            <div class="card-body">
                {{ include('admin/user/_form.html.twig', {'button_label': 'Create'}) }}

            </div>
            <div class="card-footer">
                <a href="{{ path('user_index') }}" class="btn btn-dark btn-sm float-right">
                    <i class="fa fa-arrow-left"></i> back to list
                </a>
            </div>
        </div>

    </div>


{% endblock %}
