{% extends 'admin/SubTwig/base.html.twig' %}

{% block title %}Edit Food{% endblock %}

{% block body %}
<div class="col-md-12">
    <div class="card">
        <div class="card-header">
            <strong>Edit Food</strong>
            <a class="btn btn-sm btn-dark float-right" href="{{ path('order-history') }}">
                <i class="fa fa-arrow-left"></i> back to list
            </a>
        </div>
        <div class="card-body">

    {{ include('admin/order/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
        <div class="card-footer">
    <a href="{{ path('order-history') }}">back to list</a>

    {{ include('admin/order/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
