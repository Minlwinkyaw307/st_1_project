{% extends('admin/SubTwig/base.html.twig') %}

{% block title %}New Food{% endblock %}

{% block body %}
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <strong>Create New Food</strong>
                <a class="btn btn-sm btn-dark float-right" href="{{ path('food_index') }}">
                    <i class="fa fa-arrow-left"></i> back to list
                </a>
            </div>
            <div class="card-body">
                {{ include('admin/food/_form.html.twig') }}
            </div>
            <div class="card-footer">
                <button class="btn btn-sm btn-primary float-right">
                    <i class="fa fa-dot-circle-o"></i> Create
                </button>
                </form>
            </div>
        </div>
    </div>



{% endblock %}
